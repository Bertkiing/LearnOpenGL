<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bertking.com.openglproject">

    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION" />
    <!-- 访问网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许应用程序读写数据在外部存储设备（主要是SD卡） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 状态获取 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 请求访问使用照相设备 -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TextRxJavaActivity" />
        <activity android:name=".activity.RxjavaThreadActivity" />
        <activity android:name=".BethesthActivity" />
        <activity android:name=".activity.RxJavaOperatorActivity" />
        <activity android:name=".activity.Scroller1Activity" />
        <activity android:name=".activity.BaseActivity" />
        <activity android:name=".activity.CreditScoreActivity" />
        <activity
            android:name=".activity.OrientationActivity"
            android:configChanges="keyboardHidden|orientation"
            android:screenOrientation="portrait" />
        <activity android:name=".activity.TestAAActivity" />
        <activity android:name=".activity.ApacheTestActivity" />
        <activity android:name=".activity.AsyncTaskActivity" />
        <activity android:name=".activity.ProxyActivity" />
        <activity android:name=".activity.VegaActivity" />
        <activity android:name=".kotlin.Kotlin1Activity"></activity>
    </application>

</manifest>